<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Doggable" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Doggable" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag "modernizr" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>

    <!--[if lt IE 9]>
        <%= stylesheet_link_tag "fallbacks", :media => "all" %>
    <![endif]-->

  </head>
  <body class="<%= controller_name %> <%= action_name %>">
    <header class="main_header">
      <nav>
        <div class="container">
          <%= render 'layouts/navigation' %>
        </div>
      </nav>
      <div class="plate">
        <span>Woofee</span>
      </div>
    </header>
    <div id="main" role="main">
      <div class="container">
        <div class="content">
           <div class="row">
            <div class="span12">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          </div>
          <footer>
          </footer>
        </div>
      </div> <!--! end of .container -->
      <%= debug(params) if Rails.env.development? %>
    </div> <!--! end of #main -->
  </body>
</html>